/*
    Realizzare un programma C che permetta di memorizzare i dati di massimo 10 persone,
    ogni persona è identificata dai seguenti dati:
    - cognome (max 25 char)
    - nome  (max 25 char)
    - paeseDiResidenza (max 30 char)
    - numeroCivico
    - anni

    Predisporre la struttura e quindi dichiarare nel main un vettore di 10 strutture,
    realizzare l'inserimento dei dati e la visualizzazione come lista a video.
    Predisporre la ricerca per cognome e la ricerca per paese di residenza.

    Inserimento, visualizzazione, ricerche da effettuare come FUNZIONI richiamate nel main.
    Se volete nel main si può realizzare un menu da gestire con switch-case.
*/