#include <stdio.h>

int main(){
    printf("Hello world....\n");
    return(0);  
}
/* lista comandi (seguiti da invio)
ls -> Elenca i file nella cartella in cui mi trovo.
gcc nomefile.c -> (es gcc es01.c)
    1. Se torna al prompt senza messaggio allora è stato creato il file .out pronto da eseguire
    2. Se torna al prompt con messaggi allora sono presenti errori di sintassi!
    2.1 Correggere gli errori e riprovare la compilazione con gcc.

Per eseguire il file a.out digitare:
./a.out seguito da invio.
*/